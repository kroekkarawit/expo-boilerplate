{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "baseUrl": "src", // ✅ points to /src folder
    "paths": {
      "@/components/*": [
        "components/*",
      ],
      "@/themes/*": [
        "themes/*",
      ],
      "@/themes": ["themes/index.ts"],
    }
  }
}